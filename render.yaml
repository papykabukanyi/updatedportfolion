services:
  - type: web
    name: papy-store
    runtime: node
    region: oregon
    plan: free
    branch: papys-store
    buildCommand: npm install && npm run build
    startCommand: npm run start
    autoDeploy: true
